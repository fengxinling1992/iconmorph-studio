# 本轮调整清单

## 模板参数收敛与纯色输出

- [x] 为每个模板建立仅包含必要控制项的右侧参数配置。
- [x] 双色分层仅保留顶层色、底层色与投影远近。
- [x] 线性渐变仅保留起止色与渐变角度。
- [x] 移除长阴影模板与对应导出项。
- [x] 2.5D 轻拟物仅保留挤出厚度、角度和三面配色。
- [x] 将非 3D 插画场景模板统一改为无装饰的纯色背景。
- [x] 验证模板切换、预览、移动端布局和生产构建。

## 整体渐变与控件可用性

- [x] 将所有渐变按完整 SVG 轮廓使用统一用户坐标系映射，消除分区域渐变。
- [x] 提高滑块轨道与拇指的实际点选面积。
- [x] 为每个数值参数增加可直接编辑的数值输入框。
- [x] 放大左侧模板库的风格名称、编号和卡片行高。
- [x] 验证渐变连续性、数值输入、移动端触控与生产构建。

## 两面挤出结构

- [x] 移除 2.5D 与 3D 插画场景中现有的顶面挤出层。
- [x] 为 2.5D 与 3D 插画场景建立独立的侧面与底面层。
- [x] 将 2.5D 分面配色改为正面、侧面、底面三项。
- [x] 更新文案并验证桌面、移动端、SVG/PNG 导出与生产构建。

## 连续实体挤出

- [x] 将侧面和底面改为从正面轮廓连续延展的连接挤出面。
- [x] 移除两个分面之间的断裂感并保留可单独配色的层级。
- [x] 补充连接边缘与克制高光，强化实体厚度感。
- [x] 验证 2.5D、3D 场景、移动端预览与生产构建。

## 干净双分面修正

- [x] 移除造成多层条带观感的连续过渡挤出带。
- [x] 去除 2.5D 与 3D 场景中所有挤出连接描边。
- [x] 以简洁的侧面和底面连接保持实体厚度。
- [x] 验证预览、移动端与生产构建。

## 融合双分面实体

- [x] 将侧面与底面合并为同一闭合、连续的挤出体外壳。
- [x] 在该闭合外壳中保留可独立调整颜色的侧面和底面区域。
- [x] 维持无层叠条带、无描边的干净实体表现。
- [x] 验证 2.5D、3D、导出与移动端预览。

## 右下转角接缝校正

- [x] 将侧面与底面的材质接缝定位至正面右下转角。
- [x] 保证右侧面完整延伸，不在侧面中段出现分割线。
- [x] 验证 2.5D、3D、移动端预览与生产构建。

## 动态角到角接缝

- [x] 将水平材质边界替换为侧面角到底面角的斜线接缝。
- [x] 令该接缝随挤出角度实时旋转与更新。
- [x] 验证 2.5D、3D、移动端预览及生产构建。

## 窄底面材质带

- [x] 将过大的底面半平面裁切改为仅覆盖底部外露挤出带的多边形。
- [x] 保持侧面完整且不让底面侵入正面轮廓区域。
- [x] 验证 2.5D、3D、移动端预览与生产构建。

## 正面底面无缝衔接

- [x] 为底面局部多边形增加与正面底边的微小安全重叠。
- [x] 保持动态角到角接缝和独立侧面、底面颜色不受影响。
- [x] 验证 2.5D、3D、移动端预览与生产构建。

## 默认 3D 场景素材

- [x] 将用户提供的底座、玻璃球、方块、飘带和环绕素材上传为可部署的静态资产。
- [x] 将它们编排为 3D 插画场景的默认底座与周边装饰。
- [x] 移除旧 3D 场景背景、默认圆点和路径装饰。
- [x] 验证新场景的透视层级、移动端预览与生产构建。

## 角度驱动的边缘挤出

- [x] 在 90°–270° 区间自动将底边挤出切换为顶边挤出。
- [x] 在其余角度保留底边挤出，并保持侧面和第二分面连续衔接。
- [x] 同步 2.5D 与 3D 场景的分面位置、文案、预览和导出构建。

## 四象限双分面映射

- [x] 实现 0°–90° 右边+底边、90°–180° 底边+左边的双分面组合。
- [x] 实现 180°–270° 左边+顶边、270°–360° 顶边+右边的双分面组合。
- [x] 同步动态颜色标签、2.5D/3D 几何、预览与导出验证。

## 顶边双顶角起始

- [x] 将顶边分面的起点扩展至 SVG 左上和右上两个顶角。
- [x] 保持顶边与相邻侧边在外部挤出终点的连续衔接。
- [x] 验证 180°–360° 顶边组合、移动端与生产构建。

## 真实轮廓顶角锚点

- [x] 解析源 SVG 的实际可见边界，不再依赖固定比例内缩。
- [x] 从实际边界的左上与右上顶角生成顶边挤出。
- [x] 验证矩形、圆形与复杂路径图标的顶边锚点、移动端与导出构建。

## 异形轮廓等距挤出

- [x] 从源 SVG 的实际轮廓采样几何，支持圆形、圆角和异形路径。
- [x] 沿当前挤出向量生成等距的连续轮廓外壳，而非外接矩形分面。
- [x] 在四个角度象限中保留两块相邻材质面的独立颜色控制。
- [x] 合并同一材质面的连续采样片段，消除同色分段之间的抗锯齿细缝。
- [x] 验证内置圆形、圆角、异形 SVG、移动端预览和导出构建。

## 30° 等角投影与分面明暗

- [x] 将 2.5D 与 3D 的投影几何对齐为 30° 等角投影。
- [x] 应用 `matrix(0.7071, 0.4082, -0.7071, 0.4082, 0, 0)` 的菱形顶面变换规则。
- [x] 建立顶面最亮、右侧面次亮、左侧面最暗的三档明暗材质体系。
- [x] 验证等角构图、响应式预览与生产构建。

## 上传图标与复杂轮廓挤出

- [x] 确保上传 SVG 无需额外操作即可继承当前默认三档分面配色。
- [x] 为开放路径、极细路径和多子路径建立稳定的挤出处理。
- [x] 抑制尖角、重复轮廓或不连续路径导致的异常尖刺与错误外扩。
- [x] 验证尖角、多路径、复杂轮廓、移动端预览及导出构建。

## 复杂轮廓安全模式

- [x] 基于路径数量、窄边和轮廓密度识别尖刺风险。
- [x] 自动收窄复杂图标的有效挤出厚度，普通图标保持原始厚度。
- [x] 增加安全模式开关与当前生效厚度提示。
- [x] 验证尖角、多子路径、移动端预览与导出构建。

## 3D 套件切换与斜切等距挤出

- [x] 锁定当前 2.5D 参数与渲染逻辑，不修改其效果。
- [x] 以默认底座搭配“玻璃球/方块”和“飘带/环绕”两组二选一装饰套件。
- [x] 增加 3D 场景默认装饰套件切换控件，并保留用户自定义素材覆盖能力。
- [x] 将 3D 主体斜切 30° 后按真实矢量轮廓等距挤出固定厚度。
- [x] 验证两套场景、3D 斜切挤出、移动端预览和生产构建。

## 2.5D/3D 独立挤出与右固定斜切

- [x] 将 2.5D 与 3D 的挤出厚度和角度拆分为两套独立参数。
- [x] 保持当前 2.5D 几何、默认值与控制项不变。
- [x] 修正 3D 主体为右边固定、左边向上斜切的投影。
- [x] 让 3D 挤出厚度和角度只作用于 3D 场景主体。
- [x] 验证独立参数、3D 斜切、移动端预览与导出构建。

## 3D 预设与装饰避让

- [x] 为 3D 斜切提供 20°、30°、40° 快捷预设。
- [x] 依据 3D 主体高度自动调整默认装饰的垂直位置。
- [x] 将玻璃球和方块装饰缩小至当前显示尺寸的约四分之一。
- [x] 验证预设切换、避让效果、移动端预览与导出构建。

## 可空选装饰与所见即所得下载

- [x] 将“物件装饰”更名为“点缀装饰”，将“动势装饰”更名为“围绕装饰”。
- [x] 允许点缀装饰和围绕装饰分别选择“无”。
- [x] 将默认点缀布局改为左小、右大的同类玻璃球或方块。
- [x] 确保单张与批量 SVG 下载包含当前可见的所有效果、素材与装饰。
- [x] 验证全部装饰状态、下载结果、移动端和生产构建。

## 3D 厚度范围与装饰位置精调

- [x] 将仅 3D 插画场景的挤出厚度上限扩展为 200，并保持 2.5D 参数不变。
- [x] 为点缀装饰和围绕装饰分别增加可直接输入的高度控制。
- [x] 移除本轮新增的 3D 插画视觉背景层，不影响场景底座与用户提供的素材。
- [x] 收窄点缀装饰的左右尺寸差，同时保留左小右大的层级。
- [x] 验证 3D 参数、全部装饰状态、移动端预览、SVG/PNG 导出与生产构建。

## 图标组件库与顶部导出流程

- [x] 复核已应用的品牌、标题和工作台文字调整，并移除用户标注的冗余顶栏元素。
- [x] 将顶部“导入 SVG”入口改为“导出结果”入口，并将导出模块移入其触发的弹框。
- [x] 为左侧图标组件库建立默认分组展示骨架，等待用户提供实际图标库文件后载入分组。
- [x] 去除变体卡右上角的蓝绿／珊瑚装饰。
- [x] 提升滑块轨道和拇指的实际触控高度。
- [x] 验证桌面、移动端、弹框导出与生产构建。

## 工作台尺度与文字精简

- [x] 手动定位并移除用户标注的冗余文字。
- [x] 适度放大桌面工作台的主体、预览与参数视觉尺度，收紧空白区域。
- [x] 保持移动端不发生横向溢出，并验证生产构建。

## 用户 SVG 图标库接入

- [x] 解析用户提供的 `iconfont.html`，提取图标名称、SVG 内容与原始分组信息。
- [x] 用正方形图标卡片与底部名称替换左侧当前的列表呈现。
- [x] 按图标库分组组织左侧组件库，并接入当前 SVG 风格化选择。
- [x] 提供按图标名称和分组过滤的搜索框。
- [x] 验证分组、搜索、卡片选择、移动端与生产构建。

## 3D 场景默认挤出参数

- [x] 将 3D 插画场景默认挤出厚度设为 92 px。
- [x] 将 3D 插画场景默认挤出角度设为 325°，保持 2.5D 参数不变。
- [x] 验证默认场景预览与生产构建。

## 工作台预览与模板布局重排

- [x] 将源图卡区域收窄，并把其下方风格模板改为纵向排列。
- [x] 将桌面变体预览改为单行三列，并移除变体区指定文字描述。
- [x] 移除源图卡内冗余描述、左侧新增按钮与基于参数刷新的按钮。
- [x] 让图标库分组标签折行展示且不遮挡，并在品牌标识后添加“SVG图标风格化工具”。
- [x] 验证桌面与移动端的结构、交互及生产构建。

## 3D 场景独立材质与镂空规则

- [x] 将 3D 场景主体、双分面与材质读数拆为独立参数，不再影响其他风格。
- [x] 移除 3D 场景透明度控制，并固定场景主体材质为 100% 不透明。
- [x] 将 3D 场景内部镂空统一填充为 `#FFFFFF`。
- [x] 只对 3D 图标的真实外轮廓生成挤出厚度，内部镂空不参与挤出。
- [x] 验证独立配置、预览、SVG/PNG 导出和生产构建。

## 3D 场景内部镂空独立填充

- [x] 识别 3D 场景主体中的内部闭合镂空轮廓，并与外轮廓颜色层分离。
- [x] 将内部闭合镂空作为独立 `#FFFFFF` 顶层填充，避免被主体渐变和外层样式覆盖。
- [x] 验证镂空不参与挤出，且不改变外轮廓的主体/分面颜色及 SVG、PNG 导出。

## 当前风格参数重置

- [x] 将右侧参数面板的刷新按钮绑定为当前已选风格的参数重置操作。
- [x] 保留当前图标、已选模板和其他风格的独立参数状态。
- [x] 验证每种风格重置后的默认值与生产构建。

## 2.5D 内部镂空独立填充

- [x] 将 2.5D 轻拟物的内部闭合镂空拆为独立 `#FFFFFF` 正面顶层。
- [x] 保持既有 2.5D 主体颜色、双分面、厚度与角度不变，并排除镂空挤出。
- [x] 验证 2.5D 预览、SVG/PNG 导出和生产构建。

## 磨砂玻璃与可调镂空颜色

- [x] 为柔和玻璃拆分专属配色与材质参数，形成更接近微软磨砂玻璃的半透明、柔化和边缘高光效果。
- [x] 将双色分层纳入独立镂空顶层规则，镂空不参与分层偏移。
- [x] 为双色分层、2.5D 轻拟物和 3D 插画场景增加独立可调的镂空颜色。
- [x] 保持双色、2.5D 与 3D 当前默认主体、分面与几何参数不变。
- [x] 验证玻璃、双色、2.5D、3D 的预览、SVG/PNG 导出与生产构建。

## 两级图标资源导航

- [x] 将左侧资源入口拆分为“图标库”和“导入 SVG”两大类。
- [x] 将用户上传的 SVG 独立归入“导入 SVG”，不与图标库原始分组混排。
- [x] 将图标库原始分组改为两行子分组导航，超过可见范围时横向滑动显示更多。
- [x] 验证两级切换、分组筛选、搜索、移动端和生产构建。

## 玻璃着色增强与文字子分组导航

- [x] 将柔和玻璃默认着色更新为 `#1A81FF → #8A58FE`，默认透光强度更新为 82%。
- [x] 将柔和玻璃百分比类参数的可调范围统一扩展为 0–100%。
- [x] 将图标库子分组改为纯文字 Tab，移除分组数量并以 8 px 间距的竖杠分隔。
- [x] 验证玻璃预览、两行子分组滚动、响应式布局与生产构建。

## 3D 挤出主体垂直居中

- [x] 基于 3D 主面与挤出厚度的整体可见边界计算垂直居中偏移。
- [x] 让挤出后的 3D 图标整体在统一底座上保持垂直对齐，并同步装饰避让。
- [x] 验证默认、较小和较大厚度及多个挤出方向下的主体对齐与生产构建。

- [x] 统一上传 SVG 的规范画布与 `preserveAspectRatio` 策略，使不同轮廓在卡片内完整显示。
- [x] 将模板库移动至上传 SVG 后的左侧源图卡片下方区域。
- [x] 将默认渐变更新为 `#A696FC` 至 `#67A7FB`，方向固定为左上至右下。
- [x] 使 2.5D 轻拟物复用 3D 插画场景的分层挤出厚度实现。
- [x] 为 2.5D 顶面、侧面与正面增加独立颜色控制，并保持导出效果一致。
- [x] 验证桌面与移动端布局、预览效果及生产构建。

## 卡片背景、2.5D 配色与直接下载

- [x] 将所有风格卡片预览背景统一为纯色 `#F1F2F6`。
- [x] 为 2.5D 正面增加独立渐变/自定义颜色，默认 `#1A81FF → #8A58FE`。
- [x] 修复“导出 → 打包下载”，让生成的 SVG/PNG 直接通过浏览器下载器保存。
- [x] 验证卡片显示、2.5D 参数与直接下载流程。


## 交付前补强验证

- [x] 移除 2.5D 面板中不再生效的“正面底色”控件。
- [x] 通过真实浏览器加载 smoke、导出代码回归测试与生产构建验证 ZIP/单张下载触发路径（实际生成 `ic-add-alert-48px-duotone@3x.png` 与 `iconmorph-ic-add-alert-48px.zip`）。


## 下载触发严格回归

- [x] 为 `downloadBlob` 增加可执行测试，覆盖标准浏览器 anchor 下载触发。
- [x] 为单张下载与 ZIP 打包下载路径补充可审查的触发验证记录（Chromium CDP 实际点击并核对两类文件落盘）。


## 子分组 Tab 间距精确修正

- [x] 将桌面与移动端子分组文字间距统一为严格 8 px，并保持竖杠分隔。
- [x] 重新验证子分组导航样式、类型检查与生产构建。


## 3D 场景装饰套件四项选择

- [x] 点缀装饰改为一行四项：无、玻璃球、方块、自定义上传。
- [x] 围绕装饰改为一行四项：无、飘带、环绕、自定义上传。
- [x] 自定义上传后在对应场景位置显示，并保留高度调节。
- [x] 移除最下方统一默认底座与自定义装饰覆盖配置模块。
- [x] 验证两组套件切换、自定义上传、场景预览、响应式布局与构建。


## 3D 自定义上传真实验证

- [x] 通过真实浏览器交互分别触发点缀和围绕自定义上传。
- [x] 核对自定义素材实际出现在 3D 场景对应位置，且高度参数仍可用。
- [x] 补充桌面与移动端四项布局验证证据，并重新运行检查与构建。


## 3D 交互路径严格复核

- [x] 实际点击点缀和围绕两处“自定义”按钮后再设置文件。
- [x] 验证两组自定义素材的场景坐标及高度调节会改变对应图像位置。
- [x] 在移动端视口下断言两组套件均为一行四项。


## 两组自定义装饰高度分别验证

- [x] 单独调节点缀高度，断言点缀自定义素材对应的两个 y 坐标发生变化。
- [x] 单独调节围绕高度，断言围绕自定义素材对应的 y 坐标发生变化，并区分两组节点位置。


## GitHub Private 仓库发布

- [x] 连接并推送到用户提供的仓库 `fengxinling1992/-iconmorph-studio`，并完成 Private 可见性设置。
- [x] 将当前最新项目代码推送到新仓库。
- [x] 验证远端默认分支、仓库可见性和最新提交。


## GitHub Pages 专用发布

- [x] 创建 `pages` 专用发布分支，保留 `main` 作为源码分支。
- [x] 配置 Vite 子路径与 GitHub Pages 静态资源引用。
- [x] 增加 GitHub Actions 构建并发布到 Pages 的工作流。
- [x] 验证 Actions、Pages 发布状态和最终访问地址，解决 404。


## Pages Actions 构建修复

- [x] 修复 Actions 中 pnpm 版本与 packageManager 不一致导致的构建失败。
- [x] 重新运行 Pages 工作流并验证发布地址不再返回 404。


## Pages 启用修复

- [x] 让工作流自动启用 GitHub Pages，解决 Configure Pages 的 Not Found。
- [x] 重新运行 Actions，确认站点成功发布并可访问。


## GitHub Pages 线上 404 复查

- [x] 核对 Pages 站点状态、仓库可见性、发布源和 deployment 元数据。
- [x] 修正导致 Actions 成功但站点未提供内容的 Pages 配置或发布方式。
- [x] 重新发布并验证首页、JS 和 Manus 静态资源均返回 HTTP 200。


## GitHub Pages 仓库重命名

- [x] 将 GitHub 仓库重命名为 `iconmorph-studio` 并同步本地远程地址。
- [x] 将 Pages 子路径从 `/-iconmorph-studio/` 更新为 `/iconmorph-studio/`。
- [x] 重新运行发布工作流并验证新网址首页与静态资源可访问。


## GitHub Pages 404 再次修复

- [x] 在浏览器和 HTTP 层复现新地址 404，核对 Pages API、部署状态和发布产物。
- [x] 修正实际导致工具首页不可访问的配置或产物问题。
- [x] 重新发布并验证工具首页、构建脚本、图标库和场景素材可访问。


## GitHub Pages 子路径路由修复

- [x] 让 wouter 路由识别 `/iconmorph-studio/` 子路径，避免首页进入应用内 404。
- [x] 重新构建发布并在浏览器中验证首页显示 IconMorph Studio（绕过旧缓存的版本参数链接已验证）。


## 单页入口最终兼容

- [x] 对单页工具直接渲染 Home，避免 wouter 在 GitHub Pages 子路径下误判首页。
- [x] 重新发布并用浏览器确认工具工作台可见（版本参数链接已显示完整工作台）。


## GitHub Pages 内置资源

- [x] 盘点图标库 JSON、字体/图标资源和全部 3D 场景素材清单及体积。
- [x] 将资源复制到 Pages 可打包的静态资源目录，并纳入 GitHub 分支。
- [x] 移除运行时对 Manus `/manus-storage` 的依赖，改用仓库相对资源路径。
- [x] 验证线上图标库、底座、玻璃球、方块、飘带和自定义场景资源均可加载。


## 3D 底座装饰选择

- [x] 增加底座装饰选项：无、底座 1、底座 2。
- [x] 将默认底座装饰设为底座 1，并让场景渲染随选项切换。
- [x] 验证底座切换、默认值、场景预览和生产构建。


## Pages 资源文件名补齐

- [x] 将底座 2 素材加入仓库并按渲染器路径命名。
- [x] 补齐 orbit/ribbon 的既有哈希文件名，保证所有场景套件选择可加载。
- [x] 重新发布并验证两种底座与全部默认装饰资源返回 200。



## 3D 图标中心缩放

- [x] 为 3D 插画场景增加独立的图标尺寸缩放参数。
- [x] 以 SVG 规范画布中心点为中心同步缩放主体、挤出面、镂空和高光。
- [x] 在 3D 参数面板提供可输入数值的缩放控件，并保持底座与装饰位置独立。
- [x] 补充缩放回归测试，验证预览、导出、其他风格隔离和生产构建。


## 底座2素材丢失修复

- [x] 检查底座2本地文件、GitHub pages 分支、渲染器路径和线上响应。
- [x] 恢复底座2内置素材并确保底座2切换使用正确资源。
- [x] 增加底座2资源与渲染路径回归测试。
- [x] 重新构建发布并验证线上底座2可加载、可切换。


## GitHub pages 同步确认

- [x] 将当前本地底座2恢复和 3D 图标中心缩放代码同步到 GitHub `pages` 分支。
- [x] 等待 GitHub Actions 成功完成并确认 Pages 发布提交更新。
- [x] 验证线上底座2 SVG、底座切换和 3D 图标尺寸缩放功能。


## GitHub README 文档

- [x] 根据当前实际功能编写中文项目 README，包含在线地址、功能、使用方式、开发命令和部署说明。
- [x] 将 README 提交到 GitHub 仓库并验证 GitHub 页面展示内容。


## 3D 图标主体位置调节

- [x] 为 3D 插画场景增加主体水平位置与垂直位置参数。
- [x] 让主体正面、挤出面、镂空和高光使用同一组位移变换，底座与装饰保持独立。
- [x] 在参数面板提供可输入数值的水平/垂直位置控件，并验证导出与画布裁剪。
- [x] 补充回归测试，运行测试、类型检查、构建并复核场景预览。


## 渐变主题预设与 GitHub 发布

- [x] 增加蓝紫、蓝青、橙红、黄绿、粉紫五组统一渐变预设。
- [x] 将预设接入线性渐变、柔和玻璃、2.5D 和 3D 插画场景等所有渐变相关设置，并保留手动颜色编辑。
- [x] 补充预设选择逻辑与渲染回归测试，运行类型检查、生产构建和预览验证。
- [x] 将最终版本同步到 GitHub `main`/`pages` 分支，等待 Actions 成功并核验线上网站。


## 主题预设同步挤出分面颜色

- [x] 让五组主题渐变预设同步更新 2.5D 挤出侧面与底面颜色。
- [x] 让五组主题渐变预设同步更新 3D 挤出侧面与底面颜色。
- [x] 保留手动分面颜色编辑，并补充回归测试、构建验证和 GitHub Pages 同步。
